// Package entity encapsulate business objects that are independent from business rules.
// e.g. interest calculation for a banking system.
// The objects within this package could have methods but it shouldn't be bound to the implementation which is the Interactor(use case).
package entity
